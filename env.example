# Database Configuration
# For local PostgreSQL:
# DATABASE_URL="postgresql://username:password@localhost:5432/financedb"
# For Replit:
# DATABASE_URL is automatically set by Replit

# Node Environment
NODE_ENV="development"

# Server Port (defaults to 5000 if not set)
PORT=5000

# Additional Settings
# Enable this only for local development, Replit handles this automatically
# REPL_ID=""  # When set, enables Neon database websocket support