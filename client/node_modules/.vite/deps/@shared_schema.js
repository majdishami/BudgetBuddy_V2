import {
  z
} from "./chunk-5R55QBTN.js";
import {
  date,
  decimal,
  pgTable,
  serial,
  varchar
} from "./chunk-UN5HC44F.js";
import "./chunk-WOOG5QLI.js";

// ../shared/schema.ts
var FrequencyType = z.enum([
  "DAILY",
  "WEEKLY",
  "BIWEEKLY",
  "MONTHLY",
  "QUARTERLY",
  "YEARLY",
  "ONE_TIME"
]);
var categories = pgTable("categories", {
  id: serial("id").primaryKey(),
  name: varchar("name", { length: 100 }).notNull(),
  color: varchar("color", { length: 7 }).notNull(),
  icon: varchar("icon", { length: 50 }).notNull(),
  createdAt: date("created_at").defaultNow(),
  updatedAt: date("updated_at").defaultNow()
});
var expenses = pgTable("expenses", {
  id: serial("id").primaryKey(),
  name: varchar("name", { length: 100 }).notNull(),
  amount: decimal("amount", { precision: 12, scale: 2 }).notNull(),
  date: date("date").notNull(),
  frequency: varchar("frequency", { length: 20 }).notNull(),
  categoryId: serial("category_id").references(() => categories.id).notNull(),
  createdAt: date("created_at").defaultNow(),
  updatedAt: date("updated_at").defaultNow()
});
var incomes = pgTable("incomes", {
  id: serial("id").primaryKey(),
  name: varchar("name", { length: 100 }).notNull(),
  amount: decimal("amount", { precision: 12, scale: 2 }).notNull(),
  date: date("date").notNull(),
  frequency: varchar("frequency", { length: 20 }).notNull(),
  source: varchar("source", { length: 50 }),
  createdAt: date("created_at").defaultNow(),
  updatedAt: date("updated_at").defaultNow()
});
var insertCategorySchema = z.object({
  name: z.string(),
  color: z.string(),
  icon: z.string()
});
var insertExpenseSchema = z.object({
  name: z.string(),
  amount: z.string(),
  date: z.date(),
  frequency: z.string(),
  categoryId: z.number()
});
var insertIncomeSchema = z.object({
  name: z.string(),
  amount: z.string(),
  date: z.date(),
  frequency: z.string(),
  source: z.string().optional()
});
export {
  FrequencyType,
  categories,
  expenses,
  incomes,
  insertCategorySchema,
  insertExpenseSchema,
  insertIncomeSchema
};
//# sourceMappingURL=@shared_schema.js.map
